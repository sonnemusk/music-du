# 设计稿 / 可点原型

| 文件 | 说明 |
|------|------|
| [m2-search-compare.html](./m2-search-compare.html) | M-2 搜索 IA：**方案 A**（tab 内搜索）vs **方案 B**（🔍 + 全屏层） |

本地：双击 HTML，或 `python3 -m http.server 8765 --directory docs/mockups` 后打开对应路径。

方案 **B 已是产品默认**（2026-08-12 确认，之后已合入 `main`）。本页只作对照，不要再按「先选型再写代码」执行。
